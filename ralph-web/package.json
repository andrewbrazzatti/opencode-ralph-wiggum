{
  "name": "ralph-web",
  "version": "1.0.0",
  "description": "Web interface for Ralph Wiggum",
  "main": "src/server.ts",
  "scripts": {
    "start": "bun run build:client && bun run src/server.ts",
    "dev": "bun build --watch src/client/app.ts --outdir public & bun run --watch src/server.ts",
    "build:client": "bun build src/client/app.ts --outdir public",
    "test": "bun test"
  },
  "dependencies": {
    "bun": "^1.0.0"
  },
  "peerDependencies": {
     "@th0rgal/ralph-wiggum": "^1.0.10"
  }
}
